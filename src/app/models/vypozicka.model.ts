export class Vypozicka {
  id: number;
  bookId: any;
  customerId: any;

  constructor(id: number, bookId: any, customerId: any) {
    this.id = id;
    this.bookId = bookId;
    this.customerId = customerId;
  }
}
